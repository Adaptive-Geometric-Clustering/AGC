This repository provides the implementation of Adaptive Geometric Clustering (AGC), a unified partition-based clustering method that enables each cluster center to adaptively learn its geometric type (point, plane, or general flat). The repository includes the AGC algorithm, along with experimental datasets and clustering evaluation metrics to facilitate reproducibility and performance assessment.
